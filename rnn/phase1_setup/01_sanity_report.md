# RNN study — Phase R1 sanity report

All gates run **local CPU** through the unified engine (`journal_prep/issue12_unified_pipeline/12_unified_engine.py`). No trainer code is duplicated. Family = `birnn` (bidirectional vanilla tanh RNN, the un-gated twin of the BiLSTM). Generated by `rnn/phase1_setup/01_sanity_checks.py`.

## Gate 0 — protocol asserts

- X shape (per window): (16, 5) (expect (16, 5))
- split sizes: train 2178 / val 634 / test 2094 (expect 2178 / 634 / 2094)
- train class balance: 812 pos / 1366 neg -> pos_weight 1.6823 (expect 1366/812 = 1.6823)
- test positives: 681 (expect 681)
- train-only norm shapes: mean (5,), std (5,) (expect (5,))
- **Gate 0: PASS**

## Gate 1 — parameter count & search ladder

- vanilla-RNN default (h128 / nl2 / do0.3) = **149,121** params (expect 149,121)

Search-space param ladder (bidirectional tanh RNN, proj_dim 64) — note the whole family sits **below** the BiLSTM's 594,561 (un-gated cell has ~1/4 the recurrent weights of the 4-gate LSTM):

| hidden | num_layers | params | ×BiLSTM |
|---|---|---|---|
| 64 | 1 | 17,153 | 0.03× |
| 64 | 2 | 41,985 | 0.07× |
| 128 | 1 | 50,305 | 0.08× |
| 128 | 2 | 149,121 | 0.25× |
| 256 | 1 | 165,761 | 0.28× |
| 256 | 2 | 560,001 | 0.94× |

- **Gate 1: PASS**

## Gate 2 — forward / backward on CPU

- forward output shape: (32, 1) (expect (32, 1)) — ok
- output finite (no NaN/Inf): True
- one BCE+Adam step: loss 0.8881 finite True, grads finite True
- **Gate 2: PASS**

## Gate 3 — determinism (bit-identical same-seed CPU runs)

`birnn` default, seed 42, trained twice on CPU (full protocol, early stop on val AUC):

- run 1: val F1 0.825083, acc 0.916404, AUC 0.966233, val_at_auc_best AUC 0.966233 (best epoch 13, 8s)
- run 2: val F1 0.825083, acc 0.916404, AUC 0.966233, val_at_auc_best AUC 0.966233 (best epoch 13, 8s)
- |deltas|: val.f1 0.0e+00, val.acc 0.0e+00, val.auc 0.0e+00, val_at_auc_best.auc 0.0e+00
- **Gate 3: PASS** (bit-identical)

## Gate 4 — engine parity (same engine builds the published BiLSTM)

- bilstm baseline (h128/nl2/do0.3) n_params = **594,561** (expect published 594,561) — ok
- bilstm seed 42 twice on CPU: val AUC 0.951687 / 0.951687; |delta AUC| 0.0e+00 (bit-identical)
- val AUC in sane band (>0.90): True
- **Gate 4: PASS**

> Note: issue12's `12_equivalence_check.py` already proved this engine is bit-equivalent to the published pipeline; this is a lightweight re-confirmation that the RNN study rides the same rails as the BiLSTM/GRU/Transformer.

---

## Summary: ALL GATES PASS ✅

| gate | check | result |
|---|---|---|
| G0 | protocol asserts (splits, pos_weight, norm) | PASS |
| G1 | birnn param count = 149,121 | PASS |
| G2 | forward/backward on CPU | PASS |
| G3 | same-seed CPU determinism (|Δ|=0) | PASS |
| G4 | engine builds published BiLSTM (594,561) | PASS |
